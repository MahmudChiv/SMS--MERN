import { Router } from "express";
import { dashboard } from "../../controller/adminController";

const router: Router = Router();

router.get("/dashboard", dashboard);
router.post("/inviteStudents", upload.)

export default router;
